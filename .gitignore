# Claude
.claude

# Rust
/target/

# SurrealDB data
/data/

# Node
node_modules/
web/node_modules/
web/.next/
web/out/
web/tsconfig.tsbuildinfo
.vite/

# Environment
.env
.env.local
mise.local.toml

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local cargo overrides (e.g. build.target-dir). Machine-specific: a Windows
# path here makes cargo build an invalid library path on Linux/macOS and every
# build fails before compiling. Keep it local, never commit it.
/.cargo/config.toml
