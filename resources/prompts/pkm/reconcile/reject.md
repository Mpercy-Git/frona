Some of your `replace` links did not hold up:

{{rejections}}

A `replace` means the older entry is **now false** — someone reading it today would be
misled. It needs `was` and `now` naming the exact value that changed, each copied
verbatim from its own entry.

If the older entry is still true, it is not a `replace`. Use `duplicate` only when it adds
no information; use `absorbed` when its information survives inside a broader entry or
across entries produced by a split. If neither definition clearly applies, drop the link.

Re-send the whole verdict. Keep everything that was not listed above exactly as it was;
only fix the links named. If you cannot name a value that changed, drop the link.
