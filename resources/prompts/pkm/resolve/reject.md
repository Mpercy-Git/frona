The proposed identity set is invalid: `{{proposed}}`.

Choose `canonical` from the subject `{{subject}}` or these candidates: {{candidates}}
Choose every `same_as` entry only from the candidates.

Correct every listed path or evidence error and re-send the complete decision with all four
keys: `canonical`, `same_as`, `merge_because`, and `distinct_because`. Evidence must quote
the exact supplied subject or candidate field. For a distinct entity, keep `canonical` and
`same_as` empty and explain every strong declined candidate in `distinct_because`.
