The proposal was structurally invalid:
{{rejections}}

Resubmit the complete `playbooks` array. Keep valid targets, correct only the listed errors,
and omit a memory rather than forcing uncertain ownership.
