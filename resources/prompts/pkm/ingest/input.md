Account owner: {{owner_name}} (handle @{{handle}}). Their own entity is `{{self_path}}`. Route any fact the user states about themselves — first person ("I"/"my") or by their own name — to `{{self_path}}`. Never create another entity for the owner.

Existing entities:
{{existing_entities}}

Research messages with successful non-recall tool executions:
{{research_messages}}

Transcript:

{{transcript}}

Extract memories with required entity links.
