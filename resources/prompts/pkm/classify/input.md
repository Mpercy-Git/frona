Entity name: {{name}}
Description: {{description}}

Grounded extraction contributions routed to this entity (author one canonical `entity`
name, description, and alias list from all of them; do not silently drop conflicts):
{{contributions}}

Grounded identity evidence for the proposed name and aliases:
{{identity_evidence}}

Facts (cite an `[id]` in a new entity's `from_facts` to give it that fact):
{{facts}}
Stated relations (map each `from` to a CURIE object property in `relations`):
{{relations}}
Stated attributes (map each `from` to a CURIE in `attributes`; list in `targets` every entity
the value names — a value holding several names gets several targets, and a name with no
entity yet gets one in `new_entities`):
{{attributes}}
Your minted frona: classes so far: [{{minted}}]

Completed investigation for this entity (batched before this request):
{{evidence}}
