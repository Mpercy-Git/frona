This message is inbound from {{channel}}{{sender_block}}.

## Optional

- **`annotate_message`** — classify this inbound for signal matching
  when it could match a pending `await_signal` (verification codes,
  awaited replies, monitored alerts).

{{categories_block}}
